# review/_history — completed review evidence

Superseded per-screen BUILD-REPORTs and old SHA-stamped witness captures,
moved here (2026-07-16, founder-approved lean pass) so the live `review/`
tree stays readable. Subpaths are preserved (e.g. `today/…`, `auth/…`).

**Nothing here is current.** The live tree keeps: `NEXT.md` (the one active
gate), `REVIEW-LOG.md`, `_system/` (roadmap), `arch/` (active decisions),
the BS-*/DR-* design contracts, and `wave1/` (capture round 2's write
target). New capture rounds write fresh evidence into the live tree; a
round's evidence moves here when it is superseded.
