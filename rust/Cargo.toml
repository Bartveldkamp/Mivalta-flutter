[package]
name = "mivalta-rust-bridge"
version = "0.1.0"
edition = "2021"
publish = false

[lib]
# cdylib → libmivalta_rust_bridge.so packaged into the APK.
# staticlib → required so flutter_rust_bridge can also build iOS later.
crate-type = ["cdylib", "staticlib"]

[dependencies]
# Pinned to the exact pub.dev release used on the Dart side (2.12.0).
flutter_rust_bridge = "=2.12.0"

# Git-rev pin (Day-7 — bumped to rust-engine main after PR #147 merged).
# 281d831 is the merge commit of `claude/day5-source-tier-binding`,
# bringing in VaultEngine.last_observation_source_tier + the
# rust-engine rustfmt-CI workflow fix. SSH URL because rust-engine
# is currently a private repo; HTTPS clones would need an unattended
# token. SSH lets local dev (Hetzner box, founder laptop) and CI
# (deploy key on rust-engine paired with the
# `RUST_ENGINE_DEPLOY_KEY` secret on this repo's Actions config —
# see `.github/workflows/smoke-build.yml`) share one transport.
#
# Local-dev override: to point at a working tree on disk instead of
# refetching from git, set the local patch in `.cargo/config.toml`
# at the repo root (see README — `[patch."ssh://git@github.com/..."]`
# survives `cargo update` because patches live outside the lock file).
gatc-ffi = { git = "ssh://git@github.com/Bartveldkamp/mivalta-rust-engine", rev = "281d831687f29930e318662beea6940c573f5dbf" }

# Day 3 needs JSON munging at the shim boundary (composing a
# SuggesterContext from engine state) and a "today" date for that
# context. Versions chosen to match what gatc-ffi already resolves to
# in the workspace; cargo will dedupe.
serde_json = "1"
chrono = { version = "0.4", default-features = false, features = ["clock"] }

[profile.release]
opt-level = 3
lto = "thin"
codegen-units = 1
strip = "symbols"
