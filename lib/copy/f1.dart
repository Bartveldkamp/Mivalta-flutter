// Locked F1 (frame 1) copy. CLAUDE.md flags any paraphrase or softening
// of these strings as a review finding. Single source of truth — any
// widget that needs an F1 line imports from here.

/// Rendered on the readiness screen when the engine has no
/// observations yet (i.e. `readiness_score`'s `advisories.
/// last_observation_at` is `null`). Verbatim, do NOT change.
const String kF1NoDataCopy = 'We need more data to predict recovery.';
